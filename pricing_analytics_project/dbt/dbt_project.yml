name: "pricing_analytics"
version: "1.0.0"
config-version: 2

profile: "pricing_analytics"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  pricing_analytics:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: table
    marts:
      +schema: marts
      +materialized: table
    pbi:
      +schema: pbi
      +materialized: view
