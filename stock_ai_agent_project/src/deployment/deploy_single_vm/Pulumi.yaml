name: deploy-single-vm
description: A Python program to deploy a containerized service to Google Cloud on a single VM
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
